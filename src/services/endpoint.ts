const serverUrl = "https://api-json-task-production.up.railway.app" 

const todos = "/todos"

export const endpoint = {
    todos: `${serverUrl}${todos}`
}

export const queryKeys = {
    tasks: "tasks"
}